"""
全局配置参数模块

所有可调节的参数都集中在此处，方便统一管理和修改。
"""

import os
from pathlib import Path

# ============================================================
# 路径配置
# ============================================================

# 项目根目录
BASE_DIR = Path(__file__).resolve().parent.parent

# 数据输入目录（存放 Excel 和 PDF）
DATA_DIR = BASE_DIR / "data"

# 报告输出目录
OUTPUT_DIR = BASE_DIR / "output"

# 上传文件缓存目录
UPLOAD_DIR = BASE_DIR / "uploads"

# 确保目录存在
for d in [DATA_DIR, OUTPUT_DIR, UPLOAD_DIR]:
    d.mkdir(parents=True, exist_ok=True)

# ============================================================
# 数值提取配置
# ============================================================

# 上下文窗口大小：提取数值前后各截取的字符数
CONTEXT_WINDOW: int = 50

# ============================================================
# 模糊匹配配置
# ============================================================

# 相似度阈值（0-100），低于此值视为不匹配
SIMILARITY_THRESHOLD: int = 60

# ============================================================
# 数值比对配置
# ============================================================

# 数值比对容差：允许的绝对误差范围
TOLERANCE: float = 0.01

# 中文单位与乘数映射
UNIT_MULTIPLIERS: dict[str, float] = {
    "万元": 10000.0,
    "亿元": 100000000.0,
    "百万元": 1000000.0,
    "千元": 1000.0,
    "元": 1.0,
    "%": 0.01,
}

# ============================================================
# 置信度计算权重
# ============================================================

# 模糊匹配分数权重
WEIGHT_FUZZY_SCORE: float = 0.60

# 关键词-数值距离分数权重
WEIGHT_DISTANCE: float = 0.25

# 单位一致性分数权重
WEIGHT_UNIT_CONSISTENCY: float = 0.15

# ============================================================
# 置信度等级阈值
# ============================================================

# 高置信度阈值
HIGH_CONFIDENCE_THRESHOLD: int = 80

# 低置信度阈值
LOW_CONFIDENCE_THRESHOLD: int = 0
